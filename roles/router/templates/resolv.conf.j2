# Generated by Ansible
# Use local dnsmasq instance first, then fallback to specified DNS servers
nameserver 127.0.0.1
{% for dns in dhcp.dns_servers %}
nameserver {{ dns }}
{% endfor %}
search {{ pxe.domain | default('local') }}
